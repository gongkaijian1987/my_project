{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash(chunk:*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); echo \"$INPUT\" | grep -q '\"git commit' || exit 0; cd ${CLAUDE_PROJECT_DIR:-.} && bash scripts/chunk-pre-commit-gate.sh || exit 2",
            "timeout": 900
          }
        ]
      }
    ]
  }
}

